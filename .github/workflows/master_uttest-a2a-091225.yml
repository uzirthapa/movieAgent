name: Build and Deploy Movie Agent to Azure

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Copy movie_agent.prompt to dist
        run: |
          mkdir -p dist/agents/movie-agent
          cp src/agents/movie-agent/movie_agent.prompt dist/agents/movie-agent/

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: uttest-a2a-091225
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_091225 }}
          package: .
